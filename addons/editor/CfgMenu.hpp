class GVARMAIN(menu) {
    class ADDON {
        class Settings {
            displayName = "Settings";
            
            class Camera {
                displayName = "Editor";
                action = QFUNC(openEditorSettings);
            };
        };
    };
};
