class GVARMAIN(menu) {
    class GVARMAIN(editor) {
        class Settings {
            class ADDON {
                displayName = "AI";
                action = QFUNC(openAISettings);
            };
        };
    };
};
