class GVARMAIN(toolbar) {
    class ADDON {
        class settings {
            displayName = "Settings";
            class accent {
                displayName = "Adjust Color Scheme";
                action = QUOTE([] call FUNC(openAccentPicker));
            };
        };
    };
};
