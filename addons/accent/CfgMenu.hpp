class GVARMAIN(menu) {
    class GVARMAIN(editor) {
        class settings {
            class accent {
                displayName = "Color Scheme";
                action = QUOTE([] call FUNC(openAccentPicker));
            };
        };
    };
};
