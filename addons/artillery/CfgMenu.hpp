class GVARMAIN(menu) {
    class GVARMAIN(editor) {
        class Settings {
            class ADDON {
                displayName = CSTRING(Name);
                action = QFUNC(openArtillerySettings);
            };
        };
    };
};
